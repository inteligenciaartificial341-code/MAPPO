---
topic: MAPPO -- pivo multi-tenant (destilado de PRD + espinha de arquitetura)
updated: 2026-08-25T10:54
---

- (note) Input estruturado e pre-ordenado: prd-mappo-2026-08-22 (17 FRs, glossario, UJs) + ARCHITECTURE-SPINE.md (12 ADs) ja reconciliados entre si nesta sessao. Extracao direta, sem elicitacao -- input rico.
- (capability) CAP-1 Autenticacao real por pessoa -- intent: Gestor/Tecnico autenticam com conta Firebase real, perfil verificado no servidor via membership. success: zero credencial em texto plano; alterar estado do cliente nao muda perfil aceito pelas regras. (FR-1, FR-2)
- (capability) CAP-2 Isolamento multi-tenant por workspace -- intent: cada Empresa Contratante isolada, nenhuma leitura/escrita cruza workspaces. success: Emulator Suite confirma isolamento para 5 perfis (nao-autenticado, autenticado sem workspace, gestor, tecnico, outro workspace). (FR-3, FR-4)
- (capability) CAP-3 Onboarding com Ramo e Template de Checklist -- intent: cadastro escolhe Ramo, recebe template pronto editavel. success: cadastro exige Ramo; 2+ Ramos no lancamento; edicao de uma empresa nunca afeta outra. (FR-6, FR-7, FR-8)
- (capability) CAP-4 OS com compressao de foto consistente -- intent: criar/executar OS com checklist/foto/assinatura, toda foto comprimida em todo ponto de captura. success: nenhuma foto sem compressao; documento nunca excede 1MB sem aviso visivel. (FR-9, FR-10)
- (capability) CAP-5 GPS de campo com consentimento -- intent: check-in pontual + GPS ao vivo (10h) com consentimento explicito antes da primeira ativacao. success: nenhuma ativacao sem consentimento aceito; check-in sem GPS nao bloqueia fluxo. (FR-11, FR-12)
- (capability) CAP-6 Link Publico seguro -- intent: Cliente Final acompanha sem login, com expiracao/revogacao e imune a XSS. success: link expirado/revogado nunca retorna dado; payload de script nunca executa em nenhuma tela. (FR-13, FR-14, FR-15)
- (capability) CAP-7 Sincronizacao offline-first preservada -- intent: localStorage como fonte local, Firestore como sync, motor de merge existente inalterado. success: toda sequencia offline->online hoje suportada continua identica. (FR-16)
- (capability) CAP-8 Instalacao multiplataforma via PWA -- intent: instalavel em Android/PC/iPhone, reaproveitando o codigo existente. success: prompt de instalacao no Chrome; Adicionar a Tela de Inicio funcional no iPhone. (FR-17)
- (constraint) Nenhuma mudanca em firestore.rules/indexes vai a producao sem teste previo no Emulator Suite
- (constraint) Nenhum backend novo (Cloud Functions, servico HTTP proprio) -- autorizacao vive so em firestore.rules
- (constraint) index.html e arquivo unico -- toda mudanca cirurgica, nenhum refactor amplo
- (constraint) Motor de merge existente mantem assinatura e comportamento identicos -- multi-tenant e so um parametro de caminho a mais
- (constraint) Migracao de dado real exige backup do Firestore e plano de rollback definido antes de executar
- (constraint) Nenhum commit direto em main sem autorizacao explicita do proprietario para o commit especifico
- (constraint) Nenhuma dependencia nova sem aprovacao -- nada alem do ja pinado (Firebase compat SDK 10.12.2, Leaflet 1.9.4, jsPDF 2.5.1)
- (decision) Non-goals: nao vira PM tool generica; sem monetizacao/billing; sem painel admin completo; sem onboarding self-service; sem empacotamento alem de PWA; sem migracao pra Firebase Storage; sem mais de 2 Ramos; sem recuperacao de senha self-service/login social; sem autorizacao por recurso intra-workspace; sem staging separado; sem CI/CD
- (decision) Success signal: ao menos uma Empresa Contratante do piloto usa o MAPPO na operacao diaria real e confirma que pagaria, perguntada diretamente; e nenhum dado de uma empresa aparece para outra, verificado no Emulator Suite antes de qualquer publicacao
- (assumption) Uma pessoa (uid) pertence a exatamente um workspace (herdado de AD-11 da espinha)
- (assumption) Nenhuma meta de prazo dada para inicio do piloto -- tratado como apos o bloqueante de seguranca resolvido
- (assumption) 2 Ramos do MVP: Refrigeracao/Climatizacao (confirmado, reaproveita VRF/VRV) + segundo Ramo ainda nao definido
- (assumption) Empresas que exigem app nativo desde o dia 1 ficam fora do v1
- (question) Quais regras do Firestore estao de fato publicadas hoje em producao (mappo-13a30)? Pode ser incidente de exposicao ja em andamento -- prioridade maxima, bloqueia confianca em CAP-2
- (question) Quantas empresas e quais Ramos exatos entram no piloto?
- (question) Projeto Firebase esta no plano Spark ou Blaze?
- (question) Recuperacao de senha self-service entra em alguma fase futura ou fica indefinidamente fora?
- (question) Existe requisito de prazo para retencao/expurgo de dados de GPS e historico de check-in (LGPD)?
- (question) Tecnicos ja consentiram formalmente com rastreamento de GPS ao vivo hoje, fora do app?
- (question) Testes unitarios das funcoes de merge entram nesta rodada ou ficam para depois?
- (question) MAPPO e o nome final do produto ou working title a confirmar?
- (decision) Companion spec-authored: glossary.md (11 termos do dominio, catalogo multi-item)
- (decision) Companion adotado: ARCHITECTURE-SPINE.md (12 ADs + diagramas -- conteudo HOW, nao pertence ao kernel, mas downstream precisa ler)
- (decision) Companion adotado: AGENTS.md (politicas/pitfalls do repo -- load-bearing pra qualquer implementacao)
- (decision) Companion adotado: project-context.md (plano mestre do proprietario, conforme regra explicita da skill)
- (decision) Sources (absorvidos, sem valor downstream remanescente): prd.md, os dois briefs + addendum, _audit/mappo-initial-audit.md -- todo conteudo load-bearing ja fluiu para o PRD e dai para este SPEC
- (event) Self-validate Pass 1 (coerencia, regras 1-6+8): PASS -- 8 capacidades com intent+success, 7+1 constraints todas dobram decisao real, 11 non-goals explicitos, success signal testavel/demonstravel, IDs CAP-1..8 sem gaps/duplicatas, prosa enxuta.
- (event) Self-validate Pass 2 (preservacao): 2 gaps encontrados e corrigidos -- SM-C1 (contra-metrica de nao sacrificar qualidade do template por quantidade de ramos) virou Constraint; edge case da UJ-1 (ramo sem template pronto oferece generico mais proximo) virou Assumption. Resto do conteudo load-bearing do PRD/briefs/audit confirmado presente no kernel ou nos companions adotados (spine, AGENTS.md).
- (direction) Story Breakdown autorizado pelo usuario para rodar com defaults sensatos nos campos de preferencia pessoal (spec_checkpoint/done_checkpoint/invoke_dev_with), sem parar a cada story. Duas travas continuam exigindo autorizacao explicita a cada acao: deploy/firestore.rules/commit em main; e confirmacao do OQ-1 (regras publicadas) antes de validar CAP-2 de verdade.
- (decision) Story 1: Autenticacao real + isolamento multi-tenant (CAP-1, CAP-2). Fundacional, tudo depende dela. spec_checkpoint=true, done_checkpoint=true -- pausa obrigatoria (Emulator Suite + OQ-1 antes de prosseguir).
- (decision) Story 2: Onboarding com Ramo e Template de Checklist (CAP-3). Depende da Story 1. Checkpoints em false -- risco baixo, base ja verificada.
- (decision) Story 3: Ordem de Servico com compressao de foto consistente (CAP-4). Paralelizavel com Story 2 (PRD sequenciamento), mas listada em ordem apos Story 1. Checkpoints em false.
- (decision) Story 4: GPS de campo com consentimento (CAP-5). Baixo risco arquitetural, so UI+persistencia. Checkpoints em false.
- (decision) Story 5: Link Publico seguro (CAP-6). Toca firestore.rules (expiracao) -- done_checkpoint=true pela mesma trava de regras/deploy.
- (decision) Story 6: Verificacao de nao-regressao da sincronizacao offline-first (CAP-7). E verificacao, nao feature nova (AD-5 diz nao mudar). Checkpoints em false.
- (decision) Story 7: Instalacao multiplataforma via PWA (CAP-8). Ultima por design -- PRD/espinha dizem so fazer sentido depois das bloqueantes estaveis. Checkpoints em false.
- (event) stories.yaml renderizado e validado contra o schema: 7 entradas, ids '1'..'7' unicos e prefix-free, todos os campos obrigatorios presentes, nenhum campo status. Story 1 e Story 5 com done_checkpoint=true (tocam auth/isolamento e firestore.rules respectivamente); resto false conforme autorizacao do usuario para rodar sem parar.
- (event) spec finalized
- (direction) Proprietario trouxe brain dump de 5 mudancas de interface/funcionalidade novas (pos 7 stories originais), ja organizado e confirmado em conversa: aba financeira, VRF Ramo-scoped+checklist editavel, mapa (painel recolhivel+avatar+historico), convite de prestador invertido (gestor gera codigo), pontos de entrada novos (WhatsApp+compartilhar). Modo interativo, coach em decisoes reais em aberto.
- (capability) CAP-9 -- Aba Financeira por servico/prestador: intent: gestor mantem tabela de preco editavel por tipo de servico, lanca valor por prestador/OS com status (pago/a pagar/total) + bloco de notas para adiantamentos; valor lancado fica visivel (somente leitura) pro prestador na propria OS. success: toda OS com valor lancado mostra esse valor pro prestador dela; soma de pago/a pagar/total bate por prestador e por tipo de servico; bloco de notas nao afeta o calculo de valores por OS.
- (capability) CAP-10 -- VRF Ramo-scoped + checklist editavel: intent: modulo Sistema VRF (fases, fotos, notas preservadas) so aparece pro Ramo Refrigeracao/Climatizacao; checklist de cada fase do VRF vira editavel (mesmo editor de Configuracoes->Checklist da Story 2), sem achatar a estrutura de fases; outros Ramos veem 'Criar Tarefas' no menu (reaproveita Tarefa Adicional ja existente) no lugar do VRF. success: workspace de Ramo nao-refrigeracao nunca ve nem acessa Sistema VRF; workspace de Ramo refrigeracao edita itens de cada fase sem perder a divisao por fase.
- (capability) CAP-11 -- Mapa: painel recolhivel + avatar do prestador + historico de localizacao: intent: painel de info do prestador no mapa recolhe/expande (nao cobre botao ao rolar); prestador escolhe avatar visual (asset estatico gerado uma vez, sem dependencia de runtime nova) que aparece na posicao dele -- parado em check-in fixo, 'andando' (animacao leve via CSS) em GPS ao vivo; abaixo do mapa, lista de prestadores com historico de localizacoes enviadas (dia/hora/semana). success: painel recolhido libera o botao hoje coberto; avatar escolhido aparece no lugar do marcador padrao; historico lista as entradas por prestador com data/hora legivel.
- (capability) CAP-12 -- Convite de prestador gerado pelo gestor, revogavel: intent: gestor gera codigo de convite e envia ao prestador (inverte o fluxo atual, onde o prestador gera/envia o proprio UID); prestador entra com nome + codigo; codigo valido ate o gestor revogar. Resolve o risco ja registrado (vinculo por UID sem verificacao de identidade). success: codigo revogado nunca mais autoriza novo vinculo; fluxo de colar UID manualmente deixa de ser o caminho principal.
- (capability) CAP-13 -- Pontos de entrada novos (WhatsApp + compartilhar): intent: tela de login ganha botao WhatsApp 'Sou gestor novo, gostaria de acesso' linkando pro numero do proprietario; app do gestor ganha 'Compartilhar app' no menu lateral via Web Share API nativa. success: botao WhatsApp abre conversa pre-preenchida com o numero do proprietario; Compartilhar aciona o menu nativo do dispositivo, com fallback de copiar link se a API nao existir no navegador.
- (constraint) CAP-12 muito provavelmente toca firestore.rules (hoje so gestor escreve em members/{uid}; o convite reverso precisa de um mecanismo novo de autorizacao) -- mesmo gate de sempre: Emulator Suite antes de qualquer publicacao, autorizacao explicita separada pra regra e pra commit.
- (constraint) CAP-9: valores de OS visiveis por perfil -- gestor ve tudo do workspace; prestador ve só o valor das proprias OS, nunca o de outro prestador nem o bloco de notas de adiantamento (dado financeiro sensivel, mesmo espirito de members/{uid} nao ser confiavel via campo enviado pelo cliente).
- (non-goal) CAP-9 nao e sistema de contabilidade/faturamento completo -- sem calculo de imposto, sem integracao com processador de pagamento, so registro manual de valor e status.
- (non-goal) CAP-10 nao modela um sistema de 'obra'/projeto grande generico por Ramo -- a estrutura de fases fica exclusiva do VRF/Refrigeracao; outros Ramos usam Tarefa Adicional (ja existente), nao um equivalente novo por Ramo.
- (non-goal) CAP-11 nao inclui replay de trajeto real no mapa -- avatar tem so 2 estados visuais (parado/andando), nao segue o caminho percorrido de verdade.
- (question) CAP-9: quais sao exatamente as categorias de tipo de servico da tabela de preco -- batem com o dropdown atual de 'Tipo de Servico' da OS (Instalacao Split, Manutencao Preventiva/Corretiva, Visita Tecnica), ou e uma categorizacao nova (o proprietario citou 'Conserto', que nao existe hoje)? A tabela e por Ramo ou generica pro workspace inteiro?
- (question) CAP-12: o codigo de convite gerado pelo gestor e de uso unico (1 convite = 1 prestador) ou reutilizavel (1 codigo aceita varios prestadores ate ser revogado)? Existe um convite ativo por vez, ou o gestor pode ter varios codigos abertos simultaneamente?
- (question) CAP-13: qual e o numero de WhatsApp do proprietario a usar no link (formato internacional, ex.: 55...)?
- (event) Open Questions pre-existentes revisadas: 'Quais regras publicadas' resolvida (Story 1). 'MAPPO nome final' tratado como resolvido -- usado sem contestacao em produto/codigo/marca desde entao. Demais seguem abertas, sem mudanca.
- (event) Assumption corrigida: os 2 Ramos do MVP nao sao mais 'um a definir' -- Refrigeracao/Climatizacao + Manutencao Predial Geral, os dois implementados na Story 2.
- (event) Self-validate pass 1 (coerencia): CAP-9..13 tem intent+success (regra 1); intents descrevem o que, nao como (regra 2, checado); constraints novas (CAP-12 toca rules, CAP-9 visibilidade por perfil) de fato restringem decisao (regra 3); non-goals novos explicitos por capability (regra 4); success signals concretos e verificaveis (regra 5); IDs novos sequenciais CAP-9..13, nenhum reuso (regra 6); prosa enxuta, sem decoracao (regra 8). Veredito: OK.
- (event) Self-validate pass 2 (preservacao): todo claim do brain dump do proprietario (5 itens, incluindo as 2 clarificacoes -- valor anexado na OS visivel pro prestador; manter fases do VRF; Criar Tarefas pros outros Ramos) foi mapeado em CAP-9..13, constraints ou non-goals. Nenhum wrapper-only content descartado. Veredito: OK.
- (capability) CAP-14 -- Multi-obra no VRF: intent: Sistema VRF passa de uma unica obra por workspace para uma lista de obras (empresa pode tocar mais de uma ao mesmo tempo); gestor ve lista de obras, seleciona uma, ve o checklist e os prestadores dessa obra especifica. success: workspace com 2+ obras ativas mantem progresso/fotos/notas/checkins de cada uma isolados entre si; trocar de obra selecionada nunca mistura dado de outra.
- (constraint) CAP-14 e o item de maior escopo do pacote -- vrfObra hoje e objeto unico, nao lista; virar lista reaproveita o padrao ja existente de merge por id (ITEM_LISTS, igual mappo_os), sem inventar mecanismo novo (AD-5 preservado), mas toda funcao do VRF que assume 'a obra' precisa passar a assumir 'a obra selecionada' -- superficie grande, atencao extra de desenho na story.
- (capability) CAP-15 -- Avaliacao do app (feedback do piloto): intent: empresas do piloto enviam feedback (nota + texto livre) sobre o app, visivel so pro proprietario. success: um feedback enviado aparece pro proprietario com data e nome do workspace de origem.
- (decision) CAP-9 resolvida: tabela de preco e por Ramo (mesmo padrao ja usado pro checklist -- clonada/organizada por Ramo, nao generica pro workspace). 'Conserto' foi so exemplo, categorias exatas por Ramo ficam pra decidir na hora de construir a story, seguindo o mesmo espirito do template de checklist.
- (decision) CAP-12 resolvida: codigo de convite e de uso unico -- 1 convite = 1 prestador, consumido ao vincular.
- (decision) CAP-13 resolvida: numero de WhatsApp do proprietario e 62994299385, mensagem pre-preenchida 'sou gestor, queria utilizar o mappo'.
- (event) Todas as 3 open questions bloqueantes de CAP-9/12/13 resolvidas -- pronto pra Story Breakdown.
- (decision) Story Breakdown: 7 stories novas (ids 8-14), uma por CAP-9..15, ordem de execucao = ordem numerica. spec_checkpoint=true so na Story 13 (multi-obra VRF, maior escopo do pacote); done_checkpoint=false em todas -- proprietario quer construcao autonoma, mostrar resultado antes de commitar/publicar (gate ja padrao), só chamar em duvida real emergencial.
- (event) stories.yaml verificado contra assets/stories-schema.md: 14 entradas, ids unicos e sem prefixo colidindo, todos strings entre aspas, campos obrigatorios presentes, nenhum campo status. Veredito: OK.
- (event) SPEC.md atualizado (CAP-9..15) e Story Breakdown concluidos -- 7 stories novas (8-14) em stories.yaml, sprint-status.yaml sincronizado (chaves 1-8..1-14, mesmo epic-1). Pronto para iniciar Story 8.
- (capability) CAP-16 -- Vocabulario de servico generico e editavel por Ramo, + Ramo customizado no cadastro: intent: Refrigeracao mantem todo vocabulario atual (Sistema Split, categorias de preco, campo Quantidade de splits) sem mudanca; qualquer outro Ramo (Predial existente ou customizado) recebe categorias de preco/rotulo de modulo/dropdown Tipo de Servico genericos e proprios (nao mais copia da refrigeracao), com hint 'renomeie com sua atividade especifica', 100% editavel pelo gestor (mesmo padrao ja existente de checklistConfig/precoConfig clonados). success: workspace Predial ou customizado nunca mostra 'Instalacao Split'/'Sistema Split'/'Quantidade de splits' a menos que o gestor tenha digitado isso; gestor edita cada rotulo/categoria em Configuracoes sem afetar outro workspace.
- (constraint) CAP-16: PRECO_TEMPLATES.predial hoje e copia identica de PRECO_TEMPLATES.refrigeracao (bug de origem que motivou a capacidade) -- corrigir junto. Dropdown 'Tipo de Servico' da criacao de OS (index.html:5287-5288) e HTML hardcoded, precisa virar Ramo-aware, pareado com a mesma lista de categorias da tabela de preco (nao duas listas separadas que podem divergir). Campo 'Quantidade de splits' na criacao de OS passa a aparecer so pra Refrigeracao.
- (decision) CAP-16 reverte o Non-goal anterior 'Mais de 2 Ramos com Template de Checklist pronto' -- decisao consciente do proprietario nesta conversa, nao esquecimento. Cadastro de empresa ganha opcao 'Outro, qual?' no dropdown de Ramo, revelando campo de texto livre; Ramo customizado cai automaticamente no pacote generico-editavel (checklist generico via RAMO_TEMPLATES fallback, nunca sem checklist; nunca no pacote de Refrigeracao).
- (question) CAP-16: quantidade exata de categorias de preco genericas default e o texto exato do hint 'renomeie com sua atividade especifica' ficam a criterio de quem implementar a story, mesmo espirito ja usado pro texto do template de checklist -- proprietario nao quer reconfirmar esse nivel de detalhe, so ver o resultado antes de publicar.
- (event) Self-validate pass 1 (coerencia): CAP-16 tem intent+success (regra 1); intent descreve o que, nao como (regra 2); constraint de fato restringe decisao -- Ramo customizado nunca herda pacote de Refrigeracao (regra 3); non-goal do >2 Ramos revertido explicitamente, nao removido em silencio (regra 4, decisao registrada); success signal concreto e verificavel (regra 5); CAP-16 e o proximo ID nao usado, sem reuso (regra 6); prosa enxuta (regra 8). Veredito: OK.
- (event) Self-validate pass 2 (preservacao): todo claim da conversa com o proprietario (refrigeracao intocada, pacote generico-editavel pros demais Ramos, Outro-qual-no-cadastro, checklist customizado nunca vazio, reversao do non-goal >2 Ramos) mapeado em CAP-16/constraint/decision. Nenhum wrapper-only content descartado. Veredito: OK.
- (event) SPEC.md re-derivado com CAP-16: capability + success adicionados, 2 constraints novas, non-goal >2 Ramos removido (revertido), non-goal proprio de CAP-16 adicionado, assumption dos 2 Ramos atualizada, 1 open question de detalhe de implementacao adicionada. Pronto para Story Breakdown.
- (decision) Story Breakdown: 1 story nova (id 15) pra CAP-16. spec_checkpoint=false, done_checkpoint=false -- mesmo padrao das ultimas stories, proprietario ja aprovou o desenho nesta conversa, quer ver o resultado antes de publicar, so chamar em duvida real emergencial.
- (event) stories.yaml verificado contra assets/stories-schema.md: 15 entradas, ids unicos e sequenciais (1-15), sem prefixo colidindo, todos strings entre aspas, campos obrigatorios presentes, nenhum campo status. Veredito: OK. Pronto para iniciar Story 15.
